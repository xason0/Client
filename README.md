# Client

Project space for Client work.

## Datafy Hub preview

React + Vite + Tailwind app (dashboard, profile, MTN bundles UI).

```bash
npm install
npm run dev
```

Open the URL shown in the terminal (e.g. http://localhost:5173) to preview.
